apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wordle-solver-ingress
  namespace: {{ namespace }}
spec:
  tls:
    - secretName: wordle-solver-tls
      hosts:
        - {{ hostname }}
  rules:
    - host: {{ hostname }}
      http:
        paths:
          - pathType: ImplementationSpecific
            backend:
              service:
                name: wordle-solver-service
                port:
                  name: http4s-port